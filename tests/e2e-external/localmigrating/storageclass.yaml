apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: xoa-local-migrating
provisioner: csi.xen-orchestra.marcsi.ch
parameters:
  migrating: "true"
  srsWithTag: k8s-local # Local SRs tagged accross all hosts
allowVolumeExpansion: true